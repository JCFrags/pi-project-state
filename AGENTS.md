# Project instructions

This is an early Project State CLI prototype.

- Keep `todo`, `notes`, `workplan`, and `memory` as separate semantic areas behind one CLI.
- Prefer a small dependency-free implementation until the state protocol is defined.
- Do not treat workplans as transcript storage.
- Keep command output bounded before adding large-state support.
- Update the docs when the command shape or storage ownership changes.
